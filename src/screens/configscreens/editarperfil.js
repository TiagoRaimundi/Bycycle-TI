import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const EditarPerfil = () => {
  return (
    <View style={styles.container}>
      <Text>Várias fita</Text>
    </View>
  )
}

export default EditarPerfil;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
